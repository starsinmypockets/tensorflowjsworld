const simple = require('../fitCurve.js')
const assert = require('chai').assert
let simpleScore1, simpleScore2

describe('Tests run', function () {
  it('Should be true', function () {
    assert(true)
	})
})
